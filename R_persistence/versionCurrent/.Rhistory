install.packages("TDA_1.0.tar.gz", repos = NULL, type="source")
library(TDA)  
n = 300                                                                             |if (!is.installed("igraph")){
X = circleUnif(n) 
n
n=300
n
X = circleUnif(n)
X
quit()
library("TDA")
 Diag = gridDiag( FUN=kde, h=0.3, X=X, Xlim=Xlim, Ylim=Ylim, by=by,
g( FUN=kde, h=0.3, X=X, Xlim=Xli sublevel=F, printStatus=F )
quit()
